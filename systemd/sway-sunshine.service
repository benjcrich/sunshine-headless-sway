[Unit]
Description=Headless Sway session for Sunshine streaming
After=graphical-session.target

[Service]
Type=simple
Environment=WLR_BACKENDS=headless,libinput
Environment=LIBSEAT_BACKEND=noop
Environment=WLR_LIBINPUT_NO_DEVICES=1
Environment=WLR_RENDERER=gles2
Environment=XDG_SESSION_TYPE=wayland
Environment=XDG_CURRENT_DESKTOP=sway
Environment=SWAYSOCK=%t/sway-sunshine.sock
Environment=PULSE_SINK=sink-sunshine-headless
# PULSE_SINK only covers PulseAudio-protocol clients. PipeWire-native clients
# (Steam, SDL3 games) follow the default sink — which restore-default-sink.sh
# points back at the host — so pin their streams to the stream sink directly.
Environment="PIPEWIRE_PROPS={ target.object = sink-sunshine-headless }"
ExecStartPre=-/bin/rm -f %t/sway-sunshine.sock
ExecStart=/usr/bin/sg input -c "/usr/bin/sway --config %h/.config/sway-sunshine/config"
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
